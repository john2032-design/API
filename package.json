{
  "name": "bypass-api-proxy",
  "version": "1.0.0",
  "description": "Proxy for bypass API - Vercel compatible",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": "22.x"
  }
}
