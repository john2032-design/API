{
  "name": "bypass-api-proxy",
  "version": "1.0.1",
  "description": "Proxy for bypass API - Vercel compatible",
  "main": "api/bypass.js",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "privatebin-decrypt": "^1.0.3"
  },
  "engines": {
    "node": "22.x"
  }
}
